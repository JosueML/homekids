[phases.setup]
nixPkgs = ["nodejs_22", "yarn"]

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare yarn@4.12.0 --activate",
  "yarn install --immutable"
]
cacheDirectories = [".yarn/cache"]

[phases.build]
cmds = ["yarn build"]